services:

  redis:
    image: redis:7-alpine
    container_name: sweetwagon-redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - sweetwagon

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: sweetwagon-api
    restart: unless-stopped
    environment:
      REDIS_URL: redis://redis:6379/0
      CORS_ORIGINS: http://localhost,https://localhost,http://localhost:5173
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - sweetwagon

  scraper:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: sweetwagon-scraper
    restart: unless-stopped
    environment:
      REDIS_URL: redis://redis:6379/0
    command: >
      sh -c "while true; do
        python scraper.py;
        sleep 60;
      done"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - sweetwagon

  caddy:
    image: caddy:2-alpine
    container_name: sweetwagon-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - api
    networks:
      - sweetwagon

volumes:
  redis_data:
  caddy_data:
  caddy_config:

networks:
  sweetwagon:
    driver: bridge
